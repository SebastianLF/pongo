/**
 * Created by sebs on 06/03/2015.
 */

/*function typeaheadSportsOnDashboard(){
    var custom = new Bloodhound({
        datumTokenizer: function(d) { return d.tokens; },
        queryTokenizer: Bloodhound.tokenizers.whitespace,
        remote: 'typeaheads/sports/%QUERY'
    });
    custom.initialize();
    $('#manubetform-add .sportinputdashboard').typeahead(null, {
        name: 'sports',
        displayKey: 'name',
        highlight: true,
        source: custom.ttAdapter(),
        hint: true,
        templates: {
            empty: [
                '<div class="">unable to find any</div>'
            ]
        }
    });
}

function typeaheadPaysOnDashboard(){
    var custom = new Bloodhound({
        datumTokenizer: function(d) { return d.tokens; },
        queryTokenizer: Bloodhound.tokenizers.whitespace,
        remote: 'typeaheads/pays/%QUERY'
    });
    custom.initialize();
    $('#manubetform-add .countryinputdashboard').typeahead(null, {
        name: 'pays',
        displayKey: 'name',
        highlight: true,
        source: custom.ttAdapter(),
        hint: true,
        templates: {
            empty: [
                '<div class="">unable to find any</div>'
            ]
        }
    });
}*/
